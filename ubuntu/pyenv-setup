git clone https://github.com/yyuu/pyenv.git ~/.pyenv
echo "export PYENV_ROOT=$HOME/.pynv" >> ~/.bash_profile
echo "export PATH=$PYENV_ROOT/bin:$PATH" >> ~/.bash_profile
echo 'eval "$(pyenv init -)"' >> ~/.bash_profile
source ~/.bash_profile
pyenv install 3.6.0
